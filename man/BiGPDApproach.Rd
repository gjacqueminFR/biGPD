% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biGPD.R
\name{BiGPDApproach}
\alias{BiGPDApproach}
\title{Example of biGPD approach. Compute univariate and bivariate return periods from 2 time series.}
\usage{
BiGPDApproach(
  data,
  returnLevels,
  EGPDtypes,
  initParams1,
  initParams2,
  probaQuantile,
  nbDaysPerYear,
  nbYears,
  h,
  Dparam,
  probaOccurrence
)
}
\arguments{
\item{data}{A dataframe with two columns, one for each time series.}

\item{returnLevels}{A vector of size 2, one for each time series. Return periods correspond to the return levels.}

\item{EGPDtypes}{A vector of size 2, one for each time series. Values are 1 or 4. See Naveau et al. (2016) and the package mev for further information.}

\item{initParams1}{A vector of parameters to initialize the estimation for the first time series. For type 1, it is (Kappa, Sigma, Xi), for type 4, it is (Prob, Kappa, Delta, Sigma, Xi).}

\item{initParams2}{A vector of parameters to initialize the estimation for the second time series. For type 1, it is (Kappa, Sigma, Xi), for type 4, it is (Prob, Kappa, Delta, Sigma, Xi).}

\item{probaQuantile}{A float between 0 and 1. Data above the thresholds of this probability are used to compute the bivariate exceedence probability. The value has a small impact over the probability. It should be close to 1, 0.95 is a classical value. The same value is used for both margins for simplicity.}

\item{nbDaysPerYear}{The number of days considered per year (integer).}

\item{nbYears}{The number of distinct years. Default value is 1.}

\item{h}{The parameter of non-concurrence (integer).}

\item{Dparam}{Cf documentation of the dgaps function of the exdex package. Default value is 3.}

\item{probaOccurrence}{The probability that the values are reached before the return period time. Default value to 0.63.}
}
\value{
A list with, in that order: the return period of first variable, the return period of the second variable, the bivariate return period, the non-concurrent joint excess probability, chi and chiBarre.
}
\description{
Example of biGPD approach. Compute univariate and bivariate return periods from 2 time series.
}
